// You are given an array arr(0-based indexing). The size of the array is given by n.
// You need to update an element at the given index. The arr[i] of the array is initially set to i+1.

void updateArray(int arr[], int index, int element){
    arr[index] = element;
}